# @auth/mono

A modern authentication and authorization platform built as a monorepo. Provides comprehensive user management with support for password-based authentication, WebAuthn passkeys, OAuth providers, session management, and role-based access control.

## Architecture

- **@auth/api** - Express.js backend API with Prisma ORM
- **@auth/web** - React frontend built with Vite and TanStack Router

