# @auth/web

React frontend application built with Vite, TanStack Router, and Tailwind CSS.

## Prerequisites

- Node.js 18+ or Bun
- API server running (see `@auth/api`)

## Running

```bash
bun install
bun run dev
```

Application runs on `http://localhost:5173` by default.

