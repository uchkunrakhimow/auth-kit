# @auth/api

Express.js API server for authentication and user management.

## Prerequisites

- Bun runtime
- PostgreSQL database
- Environment variables configured (see `src/env.ts`)

## Running

```bash
bun install
bun run dev
```

Server runs on `http://localhost:3000` by default (configurable via `PORT` env var).

